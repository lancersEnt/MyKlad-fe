import { Box, Typography } from '@mui/material';
import { ReactElement } from 'react';

function Klads(): ReactElement {
  return (
    <Box>
      <Typography>Klads</Typography>
    </Box>
  );
}
export default Klads;
